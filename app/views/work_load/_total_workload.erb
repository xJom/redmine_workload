<%
# Renders an accumulated workload.
# Parameters:
#  * workload: a UserWorkload instance
%>

<% workload.each do |workload_day| %>
  <td class="<%= workload_day_class workload_day %>">
    <span><%= format_workload_hours workload_day.hours %></span>
  </td>
<% end %>

